'use client'
import React from 'react'

export function CountBadge({ children, ...p }: any) {
  return <div data-component="CountBadge" style={{display:'contents'}}>{}{}</div>
}

export default CountBadge

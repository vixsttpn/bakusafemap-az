'use client'
import React from 'react'

export function SectionHeader({ children, ...p }: any) {
  return <div data-component="SectionHeader" style={{display:'contents'}}>{}{}</div>
}

export default SectionHeader

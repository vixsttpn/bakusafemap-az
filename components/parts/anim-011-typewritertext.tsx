'use client'
import React from 'react'

export function TypewriterText({ children, ...p }: any) {
  return <div data-component="TypewriterText" style={{display:'contents'}}>{}{}</div>
}

export default TypewriterText

'use client'
import React from 'react'

export function AvatarCircle({ children, ...p }: any) {
  return <div data-component="AvatarCircle" style={{display:'contents'}}>{}{}</div>
}

export default AvatarCircle

'use client'
import React from 'react'

export function GradientText({ children, ...p }: any) {
  return <div data-component="GradientText" style={{display:'contents'}}>{}{}</div>
}

export default GradientText

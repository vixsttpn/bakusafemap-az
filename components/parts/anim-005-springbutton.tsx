'use client'
import React from 'react'

export function SpringButton({ children, ...p }: any) {
  return <div data-component="SpringButton" style={{display:'contents'}}>{}{}</div>
}

export default SpringButton

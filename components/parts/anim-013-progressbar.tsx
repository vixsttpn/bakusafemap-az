'use client'
import React from 'react'

export function ProgressBar({ children, ...p }: any) {
  return <div data-component="ProgressBar" style={{display:'contents'}}>{}{}</div>
}

export default ProgressBar

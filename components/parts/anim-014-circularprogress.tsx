'use client'
import React from 'react'

export function CircularProgress({ children, ...p }: any) {
  return <div data-component="CircularProgress" style={{display:'contents'}}>{}{}</div>
}

export default CircularProgress

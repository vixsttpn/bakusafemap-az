'use client'
import React from 'react'

export function IconBadge({ children, ...p }: any) {
  return <div data-component="IconBadge" style={{display:'contents'}}>{}{}</div>
}

export default IconBadge

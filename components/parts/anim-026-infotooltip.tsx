'use client'
import React from 'react'

export function InfoTooltip({ children, ...p }: any) {
  return <div data-component="InfoTooltip" style={{display:'contents'}}>{}{}</div>
}

export default InfoTooltip

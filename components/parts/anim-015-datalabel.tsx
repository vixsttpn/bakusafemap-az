'use client'
import React from 'react'

export function DataLabel({ children, ...p }: any) {
  return <div data-component="DataLabel" style={{display:'contents'}}>{}{}</div>
}

export default DataLabel

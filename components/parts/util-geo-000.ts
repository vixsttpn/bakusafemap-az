// util-geo-000
export const toRad = (d:number) => d * Math.PI / 180
export const toDeg = (r:number) => r * 180 / Math.PI
export const midLng = (a:number,b:number) => (a+b)/2
export const midLat = (a:number,b:number) => (a+b)/2

'use client'
import React from 'react'

export function LoadingSpinner({ children, ...p }: any) {
  return <div data-component="LoadingSpinner" style={{display:'contents'}}>{}{}</div>
}

export default LoadingSpinner
